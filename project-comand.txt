npm install -g angular-cli
# or install locally
npm install angular-cli --save-dev
# make sure you have typings installed
npm install -g typings
npm install -g typescript

# angular 2
ng new <project-name>

# firebase and angularfire2
npm install angularfire2 firebase --save

# initialize git, commit
git init
git add .
git commit -m "initialized"

# firebase configuration edit in src/app/app.module.ts

# followed official angularfire 2 docs to inject angular and retrieve data from firebase db

# trying to use reactive form for submitting data (todd motto blog post)

# trying to set up the routing

# if we want to activate routing then we have to split the "app.component parts" into a main file








